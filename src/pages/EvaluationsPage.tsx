import { ClipboardList, Plus } from 'lucide-react'
import { Link } from 'react-router-dom'
import { PageHeader } from '../components/PageHeader'
import { StatusBadge } from '../components/StatusBadge'
import { useProfessionalAssessments } from '../features/assessments/hooks'
import { assessmentPhaseLabels } from '../features/assessments/questions'

export function EvaluationsPage(){const{data:items=[],isPending}=useProfessionalAssessments();return <div className="space-y-7"><PageHeader eyebrow="Seguimiento percibido" title="Evaluaciones" description="Transcripción de cuestionarios físicos. Las comparaciones son descriptivas y requieren criterio profesional." actions={<Link to="/app/pacientes" className="inline-flex items-center gap-2 rounded-2xl bg-[#0b7a75] px-4 py-3 text-sm font-black text-white"><Plus size={17}/> Elegir paciente</Link>}/><section className="overflow-hidden rounded-3xl border border-[#dce7e5] bg-white"><div className="hidden grid-cols-[1.2fr_.8fr_.8fr_.7fr_.7fr] gap-4 bg-[#f8faf9] px-6 py-3 text-[10px] font-black uppercase tracking-[.12em] text-[#71878c] md:grid"><span>Paciente</span><span>Momento</span><span>Fecha</span><span>Puntaje</span><span>Estado</span></div>{isPending?<p className="p-8 text-sm text-[#71878c]">Cargando evaluaciones…</p>:items.length===0?<p className="p-8 text-sm text-[#71878c]">Todavía no hay evaluaciones.</p>:<div className="divide-y divide-[#e8efed]">{items.map(item=>{const expected=item.instrumentVersion>=2?18:12;const maximum=item.instrumentVersion>=2?item.applicableCount*3:48;return <Link to={`/app/pacientes/${item.patientId}`} key={item.id} className="grid gap-3 px-6 py-5 hover:bg-[#f8fbfa] md:grid-cols-[1.2fr_.8fr_.8fr_.7fr_.7fr] md:items-center"><p className="flex items-center gap-2 text-sm font-black text-[#29474d]"><ClipboardList size={16} className="text-[#0b7a75]"/>{item.patientName||'Paciente'}</p><p className="text-xs text-[#60777d]">{assessmentPhaseLabels[item.phase]}</p><p className="text-xs text-[#60777d]">{item.assessmentDate}</p><p className="text-sm font-black text-[#29474d]">{item.totalScore}/{maximum}</p><StatusBadge status={item.answeredCount===expected?'completed':'partial'}/></Link>})}</div>}</section></div>}
