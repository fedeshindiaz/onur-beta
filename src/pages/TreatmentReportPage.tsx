import { ChevronLeft, FileCheck2, Printer, Save } from 'lucide-react'
import { useMemo, useState } from 'react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import { PageHeader } from '../components/PageHeader'
import { assessmentComparison } from '../features/assessments/repository'
import { usePatientAssessments } from '../features/assessments/hooks'
import { usePatientDocuments } from '../features/documents/hooks'
import { documentTypeLabels } from '../features/documents/types'
import { usePatient } from '../features/patients/hooks'
import { useCreateReport } from '../features/reports/hooks'
import { useSessionAssignments, useTreatmentCycles } from '../features/sessions/hooks'
import { useStatisticalSuggestions } from '../features/studies/hooks'

export function TreatmentReportPage(){
  const{patientId=''}=useParams();const navigate=useNavigate();const{data:patient}=usePatient(patientId);const{data:cycles=[]}=useTreatmentCycles(patientId);const{data:sessions=[]}=useSessionAssignments(patientId);const{data:documents=[]}=usePatientDocuments(patientId);const{data:assessments=[]}=usePatientAssessments(patientId);const{data:suggestions=[]}=useStatisticalSuggestions();const create=useCreateReport(patientId);const[cycleId,setCycleId]=useState('');const[summary,setSummary]=useState('');const[selectedSuggestionIds,setSelectedSuggestionIds]=useState<string[]>([]);const[error,setError]=useState('')
  const selectedCycle=cycles.find(cycle=>cycle.id===(cycleId||cycles.find(item=>item.status==='active')?.id||cycles[0]?.id));const selectedId=selectedCycle?.id??'';const cycleSessions=sessions.filter(item=>item.treatmentCycleId===selectedId);const cycleDocuments=documents.filter(item=>item.treatmentCycleId===selectedId);const cycleAssessments=assessments.filter(item=>item.treatmentCycleId===selectedId);const comparison=assessmentComparison(assessments,selectedId);const reviewedSuggestions=suggestions.filter(item=>item.patientId===patientId&&item.treatmentCycleId===selectedId&&['accepted','edited'].includes(item.status));const includedSuggestions=reviewedSuggestions.filter(item=>selectedSuggestionIds.includes(item.id))
  const snapshot=useMemo(()=>({generatedAt:new Date().toISOString(),cycle:{id:selectedId,label:selectedCycle?.label??'',startedOn:selectedCycle?.startedOn??'',status:selectedCycle?.status??''},sessions:cycleSessions.map(item=>({title:item.title,mode:item.mode,status:item.status,availableFrom:item.availableFrom,activeSeconds:item.activeSeconds,exerciseCount:item.exercises.length,initialDiscomfort:item.initialDiscomfort,finalDiscomfort:item.finalDiscomfort,perceivedDifficulty:item.perceivedDifficulty,patientComment:item.patientComment})),documents:cycleDocuments.map(item=>({type:item.documentType,date:item.documentDate,filename:item.originalFilename,description:item.description})),assessments:cycleAssessments.map(item=>({instrument:item.instrumentCode,version:item.instrumentVersion,phase:item.phase,date:item.assessmentDate,total:item.totalScore,answered:item.answeredCount,applicable:item.applicableCount,generalRating:item.generalRating,fallsCount:item.fallsCount,walkingAidUsed:item.walkingAidUsed})),comparison:comparison?{initial:comparison.initialComparableTotal,final:comparison.finalComparableTotal,maximum:comparison.maximumScore,comparedQuestions:comparison.comparedCount,difference:comparison.difference}:null,reviewedFindings:includedSuggestions.map(item=>({suggestionId:item.id,studyId:item.studyId,ruleCode:item.ruleCode,text:item.professionalText||item.summary,limitation:item.limitation,status:item.status}))}),[comparison,cycleAssessments,cycleDocuments,cycleSessions,includedSuggestions,selectedCycle,selectedId])
  const save=async(status:'draft'|'final')=>{if(!selectedId){setError('Seleccioná un ciclo.');return}setError('');try{await create.mutateAsync({patientId,treatmentCycleId:selectedId,professionalSummary:summary,snapshot,status});navigate(`/app/pacientes/${patientId}`,{state:{notice:status==='final'?'Informe final versionado.':'Borrador de informe guardado.'}})}catch(caught){setError(caught instanceof Error?caught.message:'No fue posible guardar el informe.')}}
  const complete=cycleSessions.filter(item=>item.status==='completed').length;const partial=cycleSessions.filter(item=>item.status==='partial').length
  return <div className="space-y-7 print:bg-white"><Link to={`/app/pacientes/${patientId}`} className="inline-flex items-center gap-2 text-xs font-black text-[#0b7a75] print:hidden"><ChevronLeft size={16}/> Volver al perfil</Link><PageHeader eyebrow="Informe por ciclo" title="Resumen de seguimiento" description="Documento descriptivo preparado por el profesional. ONUr no genera conclusiones clínicas automáticas." actions={<><button type="button" onClick={()=>window.print()} className="inline-flex items-center gap-2 rounded-2xl border border-[#cfddda] bg-white px-4 py-3 text-sm font-black text-[#29474d] print:hidden"><Printer size={17}/> Imprimir / PDF</button></>}/>
    <section className="rounded-3xl border border-[#dce7e5] bg-white p-6 sm:p-8"><div className="grid gap-5 sm:grid-cols-3"><div><p className="text-[10px] font-black uppercase tracking-[.14em] text-[#71878c]">Paciente</p><p className="mt-2 text-lg font-black text-[#123238]">{patient?.fullName??'Paciente'}</p></div><label className="print:hidden"><span className="text-[10px] font-black uppercase tracking-[.14em] text-[#71878c]">Ciclo</span><select className="mt-2 h-11 w-full rounded-2xl border border-[#cfddda] px-3 text-sm" value={selectedId} onChange={event=>setCycleId(event.target.value)}>{cycles.map(cycle=><option key={cycle.id} value={cycle.id}>{cycle.label}</option>)}</select></label><div><p className="text-[10px] font-black uppercase tracking-[.14em] text-[#71878c]">Período</p><p className="mt-2 text-sm font-bold text-[#29474d]">Desde {selectedCycle?.startedOn??'-'} · {selectedCycle?.status??'-'}</p></div></div>
      <div className="mt-8 grid gap-4 sm:grid-cols-4">{[['Sesiones',cycleSessions.length],['Completadas',complete],['Parciales',partial],['Documentos',cycleDocuments.length]].map(([label,value])=><div key={String(label)} className="rounded-2xl bg-[#f4f8f7] p-4"><p className="text-2xl font-black text-[#123238]">{value}</p><p className="mt-1 text-xs text-[#71878c]">{label}</p></div>)}</div>
      <div className="mt-8 grid gap-6 lg:grid-cols-2"><article><h2 className="text-lg font-black text-[#123238]">Evaluación percibida</h2>{comparison?<div className="mt-4 rounded-2xl bg-[#e8f5f2] p-5"><p className="text-2xl font-black text-[#075e5a]">{comparison.initialComparableTotal}/{comparison.maximumScore} → {comparison.finalComparableTotal}/{comparison.maximumScore}</p><p className="mt-2 text-sm text-[#3e716f]">Diferencia descriptiva: {comparison.difference>0?'+':''}{comparison.difference} puntos sobre {comparison.comparedCount} preguntas comparables.</p></div>:<p className="mt-4 text-sm text-[#71878c]">No hay una evaluación inicial y final v2 completas para comparar.</p>}<p className="mt-3 text-xs leading-5 text-[#71878c]">Comparación de respuestas autoinformadas aplicables en ambos momentos. No constituye diagnóstico ni atribuye cambios al entrenamiento.</p></article><article><h2 className="text-lg font-black text-[#123238]">Documentos del ciclo</h2><div className="mt-4 space-y-2">{cycleDocuments.length===0?<p className="text-sm text-[#71878c]">Sin documentos asociados.</p>:cycleDocuments.map(item=><div key={item.id} className="rounded-2xl border border-[#e2ebe9] p-3"><p className="text-sm font-black text-[#29474d]">{documentTypeLabels[item.documentType]}</p><p className="mt-1 text-xs text-[#71878c]">{item.documentDate} · {item.originalFilename}</p></div>)}</div></article></div>
      <article className="mt-8"><h2 className="text-lg font-black text-[#123238]">Sesiones realizadas y asignadas</h2><div className="mt-4 divide-y divide-[#e8efed]">{cycleSessions.length===0?<p className="text-sm text-[#71878c]">Sin sesiones en este ciclo.</p>:cycleSessions.map(item=><div key={item.id} className="py-3 text-sm"><div className="grid gap-2 sm:grid-cols-[1fr_.6fr_.5fr]"><strong className="text-[#29474d]">{item.title}</strong><span className="text-[#60777d]">{item.mode==='home'?'Domiciliaria':'Presencial'}</span><span className="font-bold text-[#60777d]">{item.status}</span></div>{item.initialDiscomfort!==null&&<p className="mt-2 text-xs text-[#60777d]">Auto-reporte: malestar {item.initialDiscomfort} → {item.finalDiscomfort} · dificultad {item.perceivedDifficulty}/5{item.patientComment?` · “${item.patientComment}”`:''}</p>}</div>)}</div><p className="mt-3 text-xs leading-5 text-[#71878c]">Los valores de malestar, dificultad y comentarios son autoinformados y descriptivos; no constituyen una evaluación clínica automática.</p></article>
      <article className="mt-8"><h2 className="text-lg font-black text-[#123238]">Hallazgos estadísticos revisados</h2><p className="mt-2 text-xs leading-5 text-[#71878c] print:hidden">Solo se incluyen si los seleccionás manualmente. Las sugerencias pendientes o descartadas nunca aparecen en el informe.</p>{reviewedSuggestions.length===0?<p className="mt-4 text-sm text-[#71878c]">No hay sugerencias aceptadas o editadas para este ciclo.</p>:<div className="mt-4 space-y-3">{reviewedSuggestions.map(item=><label key={item.id} className={`block rounded-2xl border p-4 ${selectedSuggestionIds.includes(item.id)?'border-[#9ccfc7] bg-[#f2faf8]':'border-[#e2ebe9] bg-white print:hidden'}`}><span className="flex items-start gap-3"><input type="checkbox" className="mt-1 print:hidden" checked={selectedSuggestionIds.includes(item.id)} onChange={event=>setSelectedSuggestionIds(current=>event.target.checked?[...current,item.id]:current.filter(id=>id!==item.id))}/><span><strong className="text-xs text-[#0b7a75]">{item.ruleCode}</strong><span className="mt-1 block text-sm leading-6 text-[#29474d]">{item.professionalText||item.summary}</span>{selectedSuggestionIds.includes(item.id)&&<small className="mt-2 block leading-5 text-[#805b16]">Limitación: {item.limitation}</small>}</span></span></label>)}</div>}</article>
      <label className="mt-8 block"><span className="text-lg font-black text-[#123238]">Resumen profesional</span><textarea className="mt-3 min-h-36 w-full rounded-2xl border border-[#cfddda] p-4 text-sm leading-6 print:border-0 print:p-0" value={summary} onChange={event=>setSummary(event.target.value)} placeholder="Texto redactado y revisado por el profesional…"/></label>
      <aside className="mt-8 rounded-2xl border border-[#f0d8a3] bg-[#fff6e4] p-4 text-xs leading-5 text-[#805b16]">ONUr organiza información y calcula resúmenes descriptivos. Este informe no diagnostica, no prescribe y debe ser revisado y firmado por un profesional habilitado.</aside>
    </section>{error&&<p role="alert" className="rounded-2xl bg-[#fceced] p-4 text-sm font-bold text-[#a94952] print:hidden">{error}</p>}<div className="flex justify-end gap-3 print:hidden"><button type="button" onClick={()=>save('draft')} disabled={create.isPending} className="inline-flex items-center gap-2 rounded-2xl border border-[#cfddda] bg-white px-5 py-3 text-sm font-black text-[#29474d]"><Save size={17}/> Guardar borrador</button><button type="button" onClick={()=>save('final')} disabled={create.isPending} className="inline-flex items-center gap-2 rounded-2xl bg-[#0b7a75] px-5 py-3 text-sm font-black text-white"><FileCheck2 size={17}/> Versionar informe final</button></div>
  </div>
}
